# Stage: generating training data sets

- Stage ID: `S_P034_01`
- Stage type: `data_acquisition`
- Stage order: 1
- Task: `T_P034_01` — training machine-learned interatomic potentials (MLIPs)

## Goal

to generate training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces

## Input

- ab initio or first-principles approaches
- molecular dynamics (MD) simulations

## Output

- training data sets

## Datasets used

- ANI-1x — training: to generate training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces
- TiO2 — training: to generate training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces
- revised MD17 — training: to generate training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces
- MD22 — training: to generate training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces
- LMNTO — training: to generate training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces

## Evidence

- P034, PDF page 1, Introduction: "They require the generation of training data sets that sufficiently cover configurational (atom positions) and compositional (atom types) spaces using, e.g., molecular dynamics (MD) simulations based on ab initio or first-principles approaches."
