# Stage: fine-tuning SevenNet-0 using reEWC method

- Stage ID: `S_P041_03`
- Stage type: `model_training`
- Stage order: 3
- Task: `T_P041_01` — fine-tuning pretrained universal machine-learning interatomic potentials

## Goal

refine the pretrained MLIP parameters using the hybrid reEWC strategy combining Experience Replay and Elastic Weight Consolidation

## Input

- pretrained SevenNet-0 model
- fine-tuning dataset
- Replay set
- Fisher Information Matrix (FIM) computed from pretrained dataset

## Output

- reEWC-fine-tuned MLIP model

## Datasets used

- LPSC fine-tuning dataset — training: refine the pretrained MLIP parameters using the hybrid reEWC strategy
- Replay set — training: refine the pretrained MLIP parameters using the hybrid reEWC strategy

## Evidence

- P041, PDF page 4: "reEWC is a hybrid approach that combines the Replay and EWC methods. In the present work, the reEWC method is implemented by adopting the mini-batch training process, like in the Replay approach, while incorporating the EWC regularization term into the loss function during parameter updates."
- P041, PDF page 12, Fine-tuning of MLIP: "Fine-tuning the pretrained MLIP starts from the SevenNet-0 parameters and train for 400 epochs using the Adam optimizer, regardless of the fine-tuning methods."
