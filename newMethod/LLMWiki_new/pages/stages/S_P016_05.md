# Stage: Inspecting materials representations via UMAP and kernel density estimation

- Stage ID: `S_P016_05`
- Stage type: `computational_validation`
- Stage order: 5
- Task: `T_P016_01` — Probing out-of-distribution generalization in machine learning for materials

## Goal

To distinguish statistically OOD from representationally OOD test data by projecting materials into learned or hand-crafted representation spaces and estimating training data density.

## Input

- Model embeddings (ALIGNN, GMP)
- Tree ensemble descriptors (XGB)
- UMAP parameters
- kernel density estimation

## Output

- UMAP projections
- density estimates for test points
- classification of test data as representationally ID or OOD

## Datasets used

- JARVIS-DFT — computational_validation: Inspecting materials representations via UMAP and kernel density estimation to distinguish statistically OOD from representationally OOD test data
- Open Quantum Materials Database (OQMD) — computational_validation: Inspecting materials representations via UMAP and kernel density estimation to distinguish statistically OOD from representationally OOD test data

## Evidence

- P016, PDF page 5, Inspecting materials representations: "To reconcile this apparent contradiction, we propose to inspect materials not from the perspective of a single human-deﬁned attribute (e.g., element identity), but from the high-dimensional representation space of materials. Furthermore, we propose to deﬁne and distinguish two types of OOD. A test set is considered statistically OOD if the statistical distribution of one or more attributes differs from the training set, whereas a test set is considered representationally OOD if the test data are outside the region of the training set when viewed from the high-dimensional representation space."
- P016, PDF page 5, Inspecting materials representations: "Uniform Manifold Approximation and Projection (UMAP)48 is used to project the high-dimensional materials representations to a two-dimensional plane. For deep learning models, materials representations (embeddings) are automatically learned during the training; embeddings of new materials can then be derived by using the trained models."
