# Stage: benchmarking models trained with PIWSL

- Stage ID: `S_P034_03`
- Stage type: `model_evaluation`
- Stage order: 3
- Task: `T_P034_01` — training machine-learned interatomic potentials (MLIPs)

## Goal

to compare models trained using the PIWSL loss with baseline models trained using the standard supervised loss only

## Input

- test data set
- models trained with PIWSL loss
- baseline models trained with standard supervised loss

## Output

- energy (E) and force (F) root-mean-square errors (RMSEs)
- performance gains

## Datasets used

- ANI-1x — test: to benchmark models trained with PIWSL
- TiO2 — test: to benchmark models trained with PIWSL
- LMNTO — test: to benchmark models trained with PIWSL

## Evidence

- P034, PDF page 5, 5.2. Benchmark Results: "We compare models trained using the PIWSL loss (see Eq. (5)) with baseline models trained using the standard supervised loss only (see Eq. (1))."
