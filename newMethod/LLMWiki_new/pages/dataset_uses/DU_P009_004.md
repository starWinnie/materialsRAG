# Dataset Use: TextEdge in fine-tuning T5 encoder with linear prediction head

- DatasetUse ID: `DU_P009_004`
- Paper: `P009`

## Research task

predicting the properties of crystalline materials using large language models (`T_P009_01`)

## Research stage

fine-tuning T5 encoder with linear prediction head (`model_training`, `S_P009_05`)

## Dataset

TextEdge (`D_P009_textedge`)

## Usage role

training

## Purpose

Fine-tune T5 encoder with linear prediction head on preprocessed text inputs to regress/classify crystal properties.

## Used fields

- crystal text description
- property labels

## Construction method

preprocessing: stopword removal, [NUM]/[ANG] replacement, [CLS] prepending

## Filter conditions

- None stated

## Sample count

Not stated.

## Availability

- Dataset: public
- Recommendable: false

## Confidence

1.0

## Evidence

- P009, PDF page 4, Data collection and analysis: "The data originally contained 145,825 crystals structure-description pairs which we randomly split into 125,825 samples for the training set, 10,000 samples for the validation set and 10,000 samples for the test set. However, when parsing the CIF files to build the graphs that are used to fine-tune GNN’s baselines, some structure files failed to be incorporated, ending up with 125,098 training samples, 9,945 validation samples, and 9,888 samples for the test set..."
- P009, PDF page 4, Data collection and analysis: "The data originally contained 145,825 crystals structure-description pairs which we randomly split into 125,825 samples for the training set, 10,000 samples for the validation set and 10,000 samples for the test set. However, when parsing the CIF files to build the graphs that are used to fine-tune GNN’s baselines, some structure files failed to be incorporated, ending up with 125,098 training samples, 9,945 validation samples, and 9,888 samples for the test set..."
