# Stage: performing zero-shot generalization to unseen space groups

- Stage ID: `S_P002_09`
- Stage type: `experimental_validation`
- Stage order: 9
- Task: `T_P002_01` — developing a single machine learning architecture that is capable of adapting its weights automatically to enforce invariance to any input space group

## Goal

To empirically validate the core hypothesis that the adaptive architecture can generalize to materials from space groups it has never seen during training.

## Input

- CFT model
- held-out dataset of inversion-symmetric space groups

## Output

- zero-shot generalization performance metrics (GB-Gap)

## Datasets used

- Materials Project — experimental_validation: To empirically validate the core hypothesis that the adaptive architecture can generalize to materials from space groups it has never seen during training.

## Evidence

- P002, PDF page 9, ZERO-SHOT GENERALIZATION TO UNSEEN SPACE GROUPS: "The core hypothesis behind our adaptive architecture is that, by explicitly parameterizing the group constraints, CFT can generalize to materials from previously unseen space groups. We test this hypothesis in a zero-shot setting by holding out all space groups containing inversion symmetry from the training set."
