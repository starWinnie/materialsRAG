# Stage: benchmarking models trained with PIWSL

- Stage ID: `S_P034_02`
- Stage type: `model_evaluation`
- Stage order: 2
- Task: `T_P034_01` — training machine-learned interatomic potentials (MLIPs)

## Goal

compare models trained using the PIWSL loss with baseline models trained using the standard supervised loss only

## Input

- test data set
- trained MLIP models
- baseline models

## Output

- energy (E) and force (F) root-mean-square errors (RMSEs)
- performance gains

## Datasets used

- ANI-1x — test: benchmarking models trained with PIWSL
- revised MD17 — test: benchmarking models trained with PIWSL

## Evidence

- P034, PDF page 5, 5.2. Benchmark Results: "We compare models trained using the PIWSL loss (see Eq. (5)) with baseline models trained using the standard supervised loss only (see Eq. (1))."
