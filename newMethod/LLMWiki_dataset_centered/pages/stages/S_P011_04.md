# Stage: fine-tuning on downstream property prediction

- Stage ID: `S_P011_04`
- Stage type: `model_training`
- Stage order: 4
- Task: `T_P011_01` — crystal property prediction

## Goal

Adapt the pre-trained model to specific crystal property prediction tasks using labeled data.

## Input

- pre-trained model
- labeled datasets (JARVIS, Materials Project)

## Output

- fine-tuned model for crystal property prediction

## Datasets used

- JARVIS-DFT — training: Fine-tuning the pre-trained model on labeled crystal property prediction tasks.
- Materials Project — training: Fine-tuning the pre-trained model on labeled crystal property prediction tasks.

## Evidence

- P011, PDF page 1, Abstract: "We pre-train models on a dataset of 380,743 unlabeled crystal structures and fine-tune them on downstream property prediction tasks."
- P011, PDF page 4, Experimental Setup: "Subsequently, we fine-tune our model on downstream crystal property prediction tasks for 500 epochs, batch size 32 and all other hyperparameters are exactly the same as the pre-training stage."
