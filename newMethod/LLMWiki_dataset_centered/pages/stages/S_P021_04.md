# Stage: evaluating predictive accuracy on crystal benchmarks

- Stage ID: `S_P021_04`
- Stage type: `model_evaluation`
- Stage order: 4
- Task: `T_P021_01` — crystal material property prediction

## Goal

assess the performance of the trained models on held-out test sets using standard metrics

## Input

- trained iComFormer model
- trained eComFormer model
- test set crystal graphs
- test set property labels

## Output

- test MAE
- test RMSE
- comparative performance results

## Datasets used

- JARVIS-DFT — benchmark: evaluate the performance of the trained models on held-out test sets using standard metrics
- Matbench — benchmark: evaluate the performance of the trained models on held-out test sets using standard metrics
- Materials Project — benchmark: evaluate the performance of the trained models on held-out test sets using standard metrics

## Evidence

- P021, PDF page 8, EXPERIMENTS: "We assess the expressiveness of our iComFormer and eComFormer models by conducting evaluations on three widely-used crystal benchmarks: JARVIS (Choudhary et al., 2020), the Materials Project (Chen et al., 2019), and MatBench (Dunn et al., 2020)."
- P021, PDF page 8, EXPERIMENTS: "For JARVIS and MP, we follow the experimental settings of Matformer (Yan et al., 2022) and PotNet (Lin et al., 2023), and use mean absolute error (MAE) as the evaluation metric."
