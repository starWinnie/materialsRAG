# Stage: evaluating prediction performance on test set using MAE and AUC

- Stage ID: `S_P009_06`
- Stage type: `model_evaluation`
- Stage order: 6
- Task: `T_P009_01` — predicting the properties of crystalline materials

## Goal

quantify accuracy of property predictions against ground truth

## Input

- test set text descriptions
- ground-truth property labels

## Output

- MAE for regression tasks, AUC for classification task

## Datasets used

- TextEdge — test: evaluate prediction performance on test set using MAE and AUC

## Evidence

- P009, PDF page 11, LLM-Prop: "For regression tasks, we train both MatBERT and LLM-Prop with the mean absolute error (MAE) loss and evaluate them in terms of MAE error... For classiﬁcation, we train them with binary cross entropy (BCE) loss and evaluate with the area under the ROC curve (AUC)."
