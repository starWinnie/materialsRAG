# Stage: training hybrid CrysCo model with EGAT and transformer architectures

- Stage ID: `S_P013_03`
- Stage type: `model_training`
- Stage order: 3
- Task: `T_P013_01` — materials property prediction

## Goal

train the hybrid model on primary properties (formation energy, band gap, energy above convex hull)

## Input

- graph representations
- compositional features
- human-extracted physical properties

## Output

- trained CrysCo model

## Datasets used

- MP21 Formation Energy Dataset — training: train the hybrid model on primary properties (formation energy)
- MP21 Energy Above Convex Hull Dataset — training: train the hybrid model on primary properties (energy above convex hull)

## Evidence

- P013, PDF page 10, Methods: "Model architecture for crystal structure The model architecture comprises three parts: the initial feature embed- dings, updating operations, and the output layers (Fig. 1). For the initial encoding, we convert the atom type, the bond distance, and the angular values from scalars to feature vectors for subsequent neural network operations... Our updating operations use Edge-Featured Graph Attention Network (EGAT) for updating both node and edge features in G8 and L(G8a)37,38,55... Our compositional model comprises two primary modules. The initial module is a transformer-based architecture that incorporates six layers of multi-head attention blocks in each layer to encode the compositional matrix into more meaningful representations as represent in CrabNet15,44."
