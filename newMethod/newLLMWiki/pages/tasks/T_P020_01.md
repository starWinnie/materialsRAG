# Task: crystal tensor property prediction

- Task ID: `T_P020_01`
- Canonical name: `crystal_tensor_property_prediction`
- Paper: `P020`

## Objective

predicting the Tensor properties of crystals, such as the dielectric tensor ε ∈R3×3, piezoelectric tensor e ∈R3×3×3, and elastic tensor C ∈R3×3×3×3

## Material scope

- crystalline materials

## Target properties

- dielectric tensor
- piezoelectric tensor
- elastic tensor

## Constraints

- O(3) equivariance
- crystal space group invariance
- tensor matrix forms
- intrinsic crystal symmetries

## Input requirements

- crystal structure M = (A, P, L)

## Output goal

- tensor matrix forms

## Research stages

1. curating a dataset (`data_acquisition`)
2. symmetry-informed crystal graph construction (`data_preparation`)
3. crystal-level equivariant feature extraction (`model_training`)
4. equivariant tensor property prediction (`model_training`)
5. crystal symmetry enforcement module (`model_training`)
6. evaluation metrics establishment (`model_evaluation`)

## Dataset uses

- curating a dataset: JARVIS-DFT (source, DU_P020_001)
- curating a dataset: curated crystal tensor property dataset (training, DU_P020_002)
- evaluation metrics establishment: curated crystal tensor property dataset (test, DU_P020_003)

## Evidence

- P020, PDF page 3, Method: "Definition 3.1 (Crystal Tensor Property Prediction). The task of crystal tensor property prediction involves predicting the Tensor properties of crystals, such as the dielectric tensor ε ∈R3×3, piezoelectric tensor e ∈R3×3×3, and elastic tensor C ∈R3×3×3×3. These properties are predicted in their tensor matrix forms, using the crystal structure M = (A, P, L) as the input."
