# MatSet-RAG：子图后处理与可证组合检索算法设计

设计日期：2026-09-11。状态：研究设计，未实现、未验证性能或新颖性；不改变现有 V3。

## 1. 结论与研究定位

建议研究“证据保持的、验证与组合求解联动的数据集检索”。候选子图不是答案，而是待验证的搜索空间。算法逐步确认 Dataset–Need 覆盖关系，只读取可能改变最优组合的证据，输出数据集组合、逐条件见证以及最优性/未完成状态。

暂定算法名：Witness-Bounded Portfolio Search，WBPS（见证约束的组合搜索）。名称仅为本项目设计标识，不主张首次提出这一名称或机制。

与现有 V3 的差异：

- V3：节点召回 → 一跳补全 → Task–Dataset 重排 → 分数聚合 → 选择。
- WBPS：候选子图及完整结构索引 → 乐观覆盖界 → 选择待验证组合 → 见证验证 → 收紧覆盖界 → 可证最优的组合与证据子图。

核心假设：对多条件、多角色、证据分散的查询，查询条件保持与组合层面的验证调度，可以减少伪覆盖，并在保持与完整结构验证相同答案的前提下减少证据读取。前者是正确性问题，后者是待验证的性能假设。

“图检索＋集合覆盖”不是足够的新颖性主张；分支定界、位图、动态规划、惰性验证也都是已知技术。论文需要证明本任务的证据语义、覆盖界构造与组合驱动验证之间产生了新的、可复现的收益。

## 2. 现有数据的真实接入点

本次只读检查 newMethod/LLMWiki_dataset_centered/raw：138 个 Dataset、40 个 Task、136 个聚合 DatasetUse 行，其中包含 336 条 usage_records。

聚合 DatasetUse 的 paper_ids、task_ids、usage_roles 等数组不能直接作为联合事实。原子证据应来自 usage_records 中的一条记录，保留 usage_record_id、paper_id、task_id、stage_id、dataset_id、usage_role、used_fields、purpose、filter_conditions 和 evidence。

材料、性质、表示尚需形成带出处的规范字段。不能把父 Task 的 target_properties 或全数据集历史字段无条件拷贝到每条使用记录。不能把“预测输出”当成“已存在的监督标签”。

## 3. 查询及见证语义

查询 Q 有 m 个必需需求 Need。不同 Need 之间 AND；一个 Need 可以有多个 OR 模式；每个模式内部的条件 AND；字段内明确可替代值 OR。

同一对象同时拥有两种性质，应表达为两个共享对象/记录范围的谓词，而不是放进一个默认 OR 的列表。拆分 Need 不能改变用户原有的联合条件。

每个模式保留绑定变量和作用域：数据集/版本、记录或样本范围、材料范围、标签来源、用途角色、输入表示。不同 Need 可以使用不同证据；同一联合模式必须由一个证据束或被明确声明且可验证的连接共同满足。

核心版本限定为：每个 Need 存在一个单记录见证，各 Need 的数据集选择可独立；不隐式加入跨数据集兼容关系。要求训练/测试样本不交叠、相同版本或可连接键时，必须另建显式连接/不等式约束，并使用扩展求解器，不能仅靠覆盖矩阵声称满足。

定义 b 为证据束，d(b) 为其数据集：

W(b,i)=1 当且仅当 b 完整满足 Need i 的某个允许模式，且公开性、可推荐性、出处与关系验证通过。

A[d,i] = 1 当且仅当存在 d(b)=d 且 W(b,i)=1。

该定义检验的是冻结知识库中有证据支持，不保证现实中所有适用数据均已收录。

匹配状态必须区分：supported（已验证支持）、contradicted（显式冲突）、unresolved（证据尚未检查）、not_established_in_KB（在声明范围穷尽后未获支持）。后两者不等于现实世界不适用。

本体规则采用白名单与方向性：ALIAS 可用于同义归一；IS_A 按查询范围使用；DERIVED_FROM/VERSION_OF 主要证明来源，不自动传播标签、规模、公开性或版本能力。

## 4. 候选子图如何进入算法

初始语义召回子图 H0 只用于优先访问。完整模式还持有所有合格 Dataset 的结构目录和证据倒排索引。遗漏在 H0 之外的实体必须仍能由目录上界表示，否则最优性只能针对 H0。

离线建立：

1. dataset_id → usage_record_id 列表；
2. (规范谓词, 值) → usage_record_id 位图；
3. 字段级来源、关系白名单与版本标识；
4. Dataset 级宽松字段摘要，供安全的候选上界；
5. 可选的同记录字段共现摘要，减少宽松摘要的假阳性。

建立摘要的原则：允许误把不满足者留在候选中，不允许漏掉实际符合冻结匹配语义的记录。索引版本、规范化规则和图版本必须一致。

已完全结构化且谓词检验便宜的部分直接做位图交集。不要为制造“自适应检索”的优势，把本来廉价的事实验证故意改成昂贵模型调用。

## 5. 覆盖下界与上界

维护两个 n×m 布尔矩阵：

- L[d,i]=1：已经找到可展示的完整见证。
- U[d,i]=1：尚未排除存在完整见证的可能；不代表实际满足。

必须始终保持 L ≤ A ≤ U。

便宜的 U 可以先允许同一 Dataset 不同记录分别满足单条件。这种跨记录拼接仅用于乐观上界，绝不能进入最终证书。随后通过同记录交集和来源验证收紧。

对模式中的单条件 c_j，宽松摘要使用“对每个 j，存在记录 b 满足 c_j”；精确验证使用“存在同一条记录 b，对每个 j 都满足 c_j”。这两个量词顺序不同，正是需要消除的伪覆盖。

共现摘要只能作为必要条件：两两字段均共现仍不保证所有条件共同成立。

结构索引不完整、图外记录尚未纳入摘要时，U 不再是全局安全上界，输出必须降级为 candidate_scope；语义分不能用作证明 U[d,i]=0 的依据。

## 6. 优化目标及证书

核心目标沿用需求稿的前两级字典序：最大化已证需求数，然后最小化不同数据集数量。重复覆盖允许，数学名称应为 minimum set cover；不要与要求每个元素恰好覆盖一次的 Exact Cover 混淆。

设 n 为冻结候选目录中的数据集数，B=n+1。对覆盖矩阵 X 定义：

F(X)=max_S [B·|union_{d in S} C_X(d)|-|S|]。

因为一个需求的奖励大于全部 n 个数据集的基数差，这一整数目标等价于上述字典序；没有调参权重。

由 L≤A≤U 得到 F(L)≤F(A)≤F(U)。F(L) 来自已证据支持的可行组合；F(U) 来自乐观放宽问题。

停止规则：F(L)=F(U) 时，已证组合的“覆盖数量＋集合基数”已经全局最优，前提是 U 覆盖完整冻结知识库目录且两次组合求解确实精确。

证据质量和精确实体偏好是后续独立的次级优化阶段。在主目标已固定的可行集合中继续优化；如未完成该阶段，只能声明主目标最优，不能宣称完整字典序最优。

次级质量可用每个 Need 所选见证的固定审计等级，并对未读见证给出保守质量上界，继续同类验证过程。不要把 LLM 自报 confidence 直接解释成正确概率。

## 7. 组合驱动的惰性验证

算法从乐观最优组合中选择尚未确定、且被其覆盖证明使用的 Dataset–Need 单元，调用确定性验证器。

验证器先从最稀疏的条件位图开始做同记录交集，再检查余下谓词、允许的关系路径、出处及公开性。可命中一条完整见证即置 L[d,i]=1；只有穷尽该单元所有合法记录和 OR 模式，才能置 U[d,i]=0。某一条记录失败不意味着整个单元失败。

一次记录检查可更新多个 Need；失败原因与已读事实缓存，避免重复 I/O 和路径检查。

调度顺序的候选研究策略：

1. 优先处理乐观最优组合中尚无已证支持的需求；
2. 优先处理潜在支持数据集最少的需求；
3. 比较把某个未定单元临时设为 0 或 1 后的上下界变化，优先检查可能最大缩小差距的单元；
4. 用估计读取成本打破平局。

第 3 项采用有限候选的一步前瞻，额外求解成本必须计入延迟。它是启发式验证顺序，不改变事实成立性，也不保证最少验证次数。若收益不抵成本，保留稀缺需求优先作为更简单方案。

```text
WBPS(Q, H0, complete_index):
    compile Q under frozen schema and relation rules
    create sound optimistic coverage U over the complete dataset directory
    create L from already verified witnesses, optionally seeded by H0
    repeat:
        S_verified, lower = exact_portfolio_solve(L)
        S_possible, upper = exact_portfolio_solve(U)
        if lower == upper:
            return S_verified, witnesses, primary_optimal_certificate
        if budget exhausted:
            return S_verified, witnesses, bounds, search_incomplete
        choose unresolved coverage cell used by S_possible's coverage proof
        perform one bounded verification step for that cell
        propagate cached successes and failures; update L and U soundly
    then, if requested, optimize evidence quality at fixed primary objective
```

优先处理缺口不等于“第一次覆盖完整就停”。即使已经用三份数据覆盖全部需求，仍可能存在尚未验证的两份数据方案，需要继续缩小最优性差距。

## 8. 一个完整示例

问题含 N1=计算数据预训练、N2=实验带隙验证。所有下列数据集及事实仅为算法示意。

已证结果：A 支持 N1，B 也支持 N1，C 支持 N2；乐观摘要认为 D 可能同时支持 N1、N2。

初始已证解是 {A,C}，2 个数据集；乐观解是 {D}，1 个数据集。算法不因为 A 和 B 的语义分高就选 {A,B}，也不直接声称 D 足够。

检查 D 的原子记录：若实验记录仅有结构而没有带隙，且所有其他可能记录已穷尽，则排除 D→N2。假设完整目录中没有其他单数据集全覆盖可能，上下界最终同为 2，{A,C} 获得最小基数证书。

若 D 的两项覆盖都有各自合法证据，则 {D} 为 1 个数据集的全覆盖解。不同 Need 允许不同见证；每个 Need 内的联合条件仍必须在自己的合法见证中成立。

最终证据子图由被选 Dataset、Need 查询节点、原子证据记录及其来源路径组成；Need 节点属于查询层，不应写回知识库冒充新事实。证据分量不必在原始论文图上连通。

## 9. 正确性、复杂度与边界

需要正式写出并验证的性质：

1. 输出可靠性：每个声称覆盖的 Need 都存在满足模式的原子见证。
2. 上下界不变量：每次更新保持 L≤A≤U。
3. 停止正确性：上下界目标相等时，主目标达到冻结目录内全局最优。
4. 有限终止：记录有限、谓词判定可终止、缓存且公平推进未定单元时，算法最终确定答案。最多 n·m 个覆盖单元被最终判定，但一个单元可能需要检查多条记录。
5. 完整覆盖不可行：只有上界也无法覆盖全部需求，才证明在冻结知识库语义下不可行；预算耗尽只能报告尚未完成搜索。

这些保证针对既定 Query Plan、规范化规则和已审计事实。LLM 查询解析错误、错误抽取、缺失知识和现实可用性不在数学保证内，必须单独评估。

当 m 较小、各需求之间没有额外跨实体约束时，普通覆盖位掩码 DP 可以 O(n·2^m) 时间求一次基数目标，内存 O(2^m)（恢复方案可另存回溯信息）。DP 是基线工具，不是创新。

若逐次最终判定一个单元且每次全量重算两次 DP，组合求解的保守开销可达 O(n^2·m·2^m)，另加验证成本。实际需要批量更新、缓存和只对受影响状态重算；不能声称惰性方法总比一次完整验证快。

复杂跨角色绑定使用独立 MILP/CP-SAT 模式；未证明时不得沿用上述 DP 复杂度或剪枝规则。

## 10. 可以形成论文贡献的部分

候选贡献 A：证据保持的多需求组合检索语义，显式刻画“单条件分别成立”与“同一见证联合成立”的区别，以及有限知识库中的未知/不支持状态。

候选贡献 B：针对该语义构造安全乐观覆盖界，并以组合最优性差距驱动证据检索，而非固定 Top-K 或固定跳数；给出可验证的停止证书。

候选贡献 C：真实材料数据集推荐中的多条件、跨记录伪覆盖、长尾需求与无完整解评测资源，提供真实原文证据和多个合法组合。

创新性还需对照惰性约束生成、CEGAR、数据库 provenance、smallest witness 和 existing set cover search。上述一般思想不能被包装成首次提出；独特算法收益必须体现于安全上界的紧度、验证计划与整体性能。

如果“完整位图/SQL 验证＋普通 DP”已经足够快，WBPS 没有减少总时间或证据访问，就不应强行以它为主要效率贡献。此时需重新评估贡献强度，不能通过人为增加验证成本或只比较弱 Top-K 基线制造优势。

## 11. 论文实验设计

### 数据及隔离

现有 40 个 Task、336 条使用记录和反复使用的 13 题只作为原型与诊断集。目标建设数百至上千条独立人工核验查询，而非只将 13 题反复改写；最终规模由独立问题覆盖与统计功效决定，不是会议硬门槛。

先按论文、重复论文组、任务模板与时间划分，再抽取知识库和生成索引。测试论文及其衍生摘要、术语补充不能进入知识库；已知数据集实体可合法出现在历史知识库中，另设数据集家族外推切分。

至少包含：单需求、2–6 个需求、AND/OR、同一记录联合条件、来源/版本区分、长尾需求、多个同样合法的最小解、知识库不可回答、字段未知、近似候选与明确冲突。跨数据集样本隔离等扩展查询单独分组。

真实查询应有人工 Query Plan 和原文 gold witness；自然语言改写只增加表达鲁棒性，不充当独立样本量。自动构造负例只在辅助压力测试中使用，并与真实测试分开报告。

### 两层评价

Oracle Query Plan：固定人工解析，检验匹配、求解和检索算法。

End-to-end：自然语言自动解析至回答，检验语言理解、覆盖和生成。报告二者差距，避免把解析改善误算成检索贡献。

### 强基线

1. 当前 V3，固定图、模型、初始候选和输出预算。
2. Dense/BM25 + rerank Top-K。
3. 结构验证 + 贪心集合覆盖。
4. 全量位图/SQL 关系验证 + 精确 DP/MILP（最重要的正确性与效率基线）。
5. OG-RAG-style 超边覆盖后投影 Dataset，明确为任务适配版。
6. SG-RAG-style 每个 Need 结构匹配后，用统一组合求解器，避免因其原生单答案目标而不公平比较。
7. 最接近的子图优化或覆盖感知方法，在相同任务输出和证据预算下适配，分别报告原版与适配边界。

所有方法共享检索知识库、规范词表和公开性规则；在同等事实信息条件下比较，不能只给本方法人工规范化字段。

### 主指标

- 完整且正确覆盖全部条件的查询比例；
- 证据支持的 Dataset–Need precision/recall；
- 跨记录伪覆盖率、硬约束违反率；
- 对所有可行解的最小基数 gap，不只与论文作者选择的集合比较；
- 最优性证书完成率及预算内上下界差距；
- 无完整解识别、未知与未完成搜索区分；
- p50/p95 总延迟、实际读取记录数、谓词检查数、组合求解次数、API 成本；
- 答案声明的证据支持率；Canonical/Family F1 作为次级兼容指标。

### 消融及压力测试

- 同记录见证 → 合并历史字段，测伪覆盖；
- 完整结构目录 → 固定语义 Top-K，测稀有需求漏失和最优性；
- 组合驱动调度 → 相关性顺序/随机/稀缺需求顺序；
- 安全上界 → 更宽松摘要，测界紧度与验证次数；
- 惰性验证 → 全量验证，测真实速度收益；
- 精确求解 → 贪心，测最小基数；
- 去除字段来源检查、版本规则、缺口报告的独立影响。

扩展实验分别增加 Dataset 数、每个 Dataset 的记录数、需求数和高连接度节点比例。复制记录只能测运行压力，不能作为真实泛化证据。

建议至少有一个独立领域或跨材料子领域验证，以区分通用算法与特定词表修补。KDD 方向突出组合检索、规模、正确性与发现任务；ACL 方向突出自然语言逻辑保持、证据忠实性、无答案判断与跨领域。两者是论文组织建议，不是录用保证。

## 12. 实施决策与退出条件

先建立独立原型，不改写 V3。第一阶段以 20–50 个人工结构化查询检验语义正确性、上下界不变量、停止条件和穷举一致性。此阶段不宣称论文效果。

第二阶段同时实现完整关系验证＋DP 强基线与 WBPS。只有在返回同等正确答案且端到端时间/读取成本有稳定改善时，才继续深化自适应检索创新。

第三阶段建立独立测试集并扩展语料，再做端到端和跨域评测。旧 13 题只保留为回归诊断。

若精确验证显著减少错误却造成严重的支持召回损失，先审计证据粒度、字段缺失和查询解析；不得简单放宽关系条件。记录“知识库未证实”的原因，并单独评估抽取与覆盖。

## 13. 相关工作定位

- [OG-RAG, EMNLP 2025](https://aclanthology.org/2025.emnlp-main.1674/)：本体超图与紧凑上下文覆盖；不是本设计首创覆盖思想。
- [SG-RAG, ACL 2026](https://aclanthology.org/2026.acl-long.1873/)：查询结构匹配与全部条件满足；本设计需要区分多实体组合、原子证据作用域和联合验证调度。
- [DCTR, AAAI 2026](https://ojs.aaai.org/index.php/AAAI/article/view/40265)：结构完整性与信息显著性的子图优化及训练；不能笼统声称此前没有子图优化。
- [CoveR / Search for Coverage, 2026](https://arxiv.org/abs/2605.28522)：学习覆盖感知检索；不能声称覆盖意识本身新颖。
- [Finding Smallest Witnesses for Conjunctive Queries, ICDT 2024](https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.ICDT.2024.24)：数据库最小见证与复杂度；本设计的最小化对象是数据集组，且需比较已有见证优化的可直接适用部分。

本次为有针对性的相关工作核对，不是穷尽性查新。上述设计的速度、准确率提升和论文新颖性均须实现、理论检查和强基线实验后确认。
